# Vercel reads VITE_* env vars at build time. Set this in the Vercel
# dashboard (Settings → Environment Variables) — point it at your
# deployed Render API URL.
#
# Do NOT commit this with a real URL — it's only an example.
VITE_API_URL=https://your-api.onrender.com
